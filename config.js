export default {
  breakpoints: {
    xs: 0,
    sm: 400,
    lg: 720,
  },
  spacing: spacing => spacing * 8,
  radius: radius => radius * 8,
  zIndex: {
    base: 0,
    above: 5,
    aboveAll: 10,
  }
}
